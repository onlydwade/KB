# auditsystem-ansible/inventory.ini
# 這是您的主機清單檔案。
# Ansible 會讀取這個檔案來決定要在哪些伺服器上執行任務。

[auditsystem_servers]
# 請將此處的 IP 位址換成您要部署的目標伺服器 IP
192.168.1.100

# 如果要在本機執行 (需要 SSH 服務)，可以使用 localhost
# localhost ansible_connection=local