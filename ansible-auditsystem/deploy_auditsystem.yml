---
- name: Deploy and Configure AuditSystem on Ubuntu Servers
  hosts: auditsystem_servers
  become: yes

  pre_tasks:
    - name: Ensure remote deployment directory exists
      ansible.builtin.file:
        path: "{{ remote_deploy_path }}"
        state: directory
        mode: '0755'

  roles:
    - role: deploy_auditsystem