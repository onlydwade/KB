<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bytefinger.toutuo.biz.user.mapper.SysPostMenuMapper">

    <resultMap type="com.bytefinger.toutuo.biz.user.domain.SysPostMenu" id="SysPostMenuResult">
        <result property="postId" column="post_id"/>
        <result property="menuId" column="menu_id"/>
    </resultMap>

    <select id="checkMenuExistPost" resultType="Integer">
        select count(1)
        from sys_post_menu
        where menu_id = #{menuId}
    </select>

    <delete id="deletePostMenuByPostId" parameterType="Long">
        delete
        from sys_post_menu
        where post_id = #{postId}
    </delete>

    <delete id="deletePostMenu" parameterType="Long">
        delete from sys_post_menu where post_id in
        <foreach collection="array" item="postId" open="(" separator="," close=")">
            #{postId}
        </foreach>
    </delete>

    <insert id="batchPostMenu">
        insert into sys_post_menu(post_id, menu_id) values
        <foreach item="item" index="index" collection="list" separator=",">
            (#{item.postId},#{item.menuId})
        </foreach>
    </insert>

</mapper> 