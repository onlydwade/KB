apiVersion: apps/v1
kind: Deployment
metadata:
  name: tt-pro-backend-auth
  namespace: tt-pro
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: tt-pro-backend-auth
    spec:
      containers:
      - name: tt-pro-backend-auth
        image: 10.0.1.135:8080/toutuo/tt_pro_backend_auth:latest
        ports:
        - containerPort: 8201
      imagePullSecrets:
      - name: tt-pro-harbor
      restartPolicy: Always
  selector:
    matchLabels:
      app: tt-pro-backend-auth
