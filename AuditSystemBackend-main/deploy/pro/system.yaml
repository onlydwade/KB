apiVersion: apps/v1
kind: Deployment
metadata:
  name: tt-pro-backend-system
  namespace: tt-pro
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: tt-pro-backend-system
    spec:
      containers:
      - name: tt-pro-backend-system
        image: 10.0.1.135:8080/toutuo/tt_pro_backend_system:latest
        ports:
        - containerPort: 8202
      imagePullSecrets:
      - name: tt-pro-harbor
      restartPolicy: Always
  selector:
    matchLabels:
      app: tt-pro-backend-system
