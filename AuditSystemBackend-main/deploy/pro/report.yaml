apiVersion: apps/v1
kind: Deployment
metadata:
  name: tt-pro-backend-report
  namespace: tt-pro
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: tt-pro-backend-report
    spec:
      containers:
      - name: tt-pro-backend-report
        image: 10.0.1.135:8080/toutuo/tt_pro_backend_report:latest
        ports:
        - containerPort: 8205
      imagePullSecrets:
      - name: tt-pro-harbor
      restartPolicy: Always
  selector:
    matchLabels:
      app: tt-pro-backend-report
