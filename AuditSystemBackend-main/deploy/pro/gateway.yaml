apiVersion: apps/v1
kind: Deployment
metadata:
  name: tt-pro-backend-gateway
  namespace: tt-pro
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: tt-pro-backend-gateway
    spec:
      containers:
      - name: tt-pro-backend-gateway
        image: 10.0.1.135:8080/toutuo/tt_pro_backend_gateway:latest
        ports:
        - containerPort: 8200
      imagePullSecrets:
      - name: tt-pro-harbor
      restartPolicy: Always
  selector:
    matchLabels:
      app: tt-pro-backend-gateway
---
apiVersion: v1
kind: Service
metadata:
  name: tt-pro-backend-gateway-svc1
  namespace: tt-pro
spec:
  type: ClusterIP
  ports:
  - port: 8200
  selector:
    app: tt-pro-backend-gateway
