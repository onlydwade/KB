apiVersion: apps/v1
kind: Deployment
metadata:
  name: tt-pro-frontend
  namespace: tt-pro
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: tt-pro-frontend
    spec:
      containers:
      - name: tt-pro-frontend
        image: 10.0.1.135:8080/toutuo/tt_pro_frontend:latest
        ports:
        - containerPort: 80
      imagePullSecrets:
      - name: tt-pro-harbor
      restartPolicy: Always
  selector:
    matchLabels:
      app: tt-pro-frontend
---
apiVersion: v1
kind: Service
metadata:
  name: tt-pro-frontend-svc1
  namespace: tt-pro
spec:
  type: ClusterIP
  ports:
  - port: 80
  selector:
    app: tt-pro-frontend
